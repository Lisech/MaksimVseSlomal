# IDEF0 Editor

Редактор диаграмм IDEF0 с современным интерфейсом на Python/Tkinter. Позволяет создавать и редактировать функциональные диаграммы IDEF0 с поддержкой иерархической структуры, стрелок, автоматической маршрутизации и других функций.

## 📋 Содержание

- [Описание](#описание)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Запуск](#запуск)
- [Сборка exe](#сборка-exe)
- [Структура проекта](#структура-проекта)
- [Использование](#использование)
- [Горячие клавиши](#горячие-клавиши)
- [Формат файлов](#формат-файлов)
- [Разработка](#разработка)
- [Тестирование](#тестирование)
- [Решение проблем](#решение-проблем)
- [Техническая документация](#техническая-документация)
- [Лицензия](#лицензия)

## 📖 Описание

IDEF0 Editor — это графический редактор для создания функциональных диаграмм по стандарту IDEF0. Приложение предоставляет интуитивный интерфейс для создания блоков, соединения их стрелками, работы с иерархической структурой диаграмм и управления свойствами элементов.

## ✨ Возможности

### Основные функции

- **Создание блоков** - Добавление функциональных блоков на диаграмму
- **Управление стрелками** - Создание и редактирование стрелок между блоками с автоматической маршрутизацией
- **Иерархическая структура** - Работа с вложенными уровнями диаграмм
- **Панель свойств** - Редактирование свойств блоков и стрелок (название, код, тип, описание, цвет, размер и т.д.)
- **Управление слоями** - Навигация по уровням иерархии диаграммы

### Дополнительные возможности

- **Темы оформления** - Светлая и темная тема
- **Масштабирование** - Увеличение и уменьшение масштаба диаграммы
- **Панорамирование** - Перемещение по холсту
- **Undo/Redo** - Отмена и повтор действий (до 50 шагов)
- **Копирование/Вставка** - Копирование блоков и стрелок
- **Сохранение/Загрузка** - Работа с файлами проектов в формате JSON
- **Автоматическая маршрутизация** - Умная прокладка стрелок с обходом блоков
- **Точки прикрепления** - Прикрепление стрелок к блокам с точным позиционированием
- **Текст на стрелках** - Добавление подписей к стрелкам

## 📦 Требования

### Системные требования

- **ОС:** Windows 7/8/10/11, Linux, macOS
- **Python:** 3.7 или выше
- **ОЗУ:** Минимум 512 MB
- **Дисковое пространство:** ~50 MB

### Зависимости Python

- **tkinter** - Входит в стандартную поставку Python (для GUI)
- **Pillow (PIL)** - Для работы с изображениями иконок

## 🚀 Установка

### 1. Клонирование репозитория

```bash
git clone <repository_url>
cd MaksimVseSlomal/IDEF0_editor
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

Или напрямую:

```bash
pip install Pillow
```

### 3. Проверка установки

Убедитесь, что все модули установлены корректно:

```bash
python -c "import tkinter; from PIL import Image; print('Все зависимости установлены')"
```

## ▶️ Запуск

### Запуск из исходного кода

```bash
cd IDEF0_editor
python main.py
```

### Запуск exe файла

Если у вас есть собранный exe файл:

```bash
dist\main.exe
```

Или двойной клик по файлу `dist\main.exe`

## 🔨 Сборка exe

### Автоматическая сборка

Используйте один из скриптов пересборки:

**Windows:**
```bash
rebuild.bat
```

**Linux/Mac:**
```bash
python rebuild.py
```

**Универсальный способ:**
```bash
python rebuild.py
```

### Ручная сборка

1. Установите PyInstaller:
```bash
pip install pyinstaller
```

2. Перейдите в директорию проекта:
```bash
cd IDEF0_editor
```

3. Соберите exe:
```bash
pyinstaller main.spec
```

4. Результат будет в папке `dist\main.exe`

### Настройки сборки

Файл `main.spec` содержит настройки сборки. Основные параметры:

- `console=True/False` - Показывать консоль при запуске (для отладки)
- `name='main'` - Имя итогового exe файла
- `datas=[('img', 'img')]` - Включаемые ресурсы (иконки)
- `hiddenimports` - Скрытые импорты модулей

## 📁 Структура проекта

```
IDEF0_editor/
│
├── main.py                 # Точка входа в приложение
├── app.py                  # Основной класс IDEF0App и GUI
├── models.py               # Модели данных (Block, Arrow, LayerManager)
├── properties.py           # Панель свойств элементов
├── styles.py               # Стили, цвета, темы, шрифты
├── main.spec               # Конфигурация PyInstaller для сборки exe
│
├── rebuild.bat             # Скрипт пересборки для Windows
├── rebuild.py              # Универсальный скрипт пересборки
├── rebuild.sh              # Скрипт пересборки для Linux/Mac
│
├── img/                    # Иконки и изображения
│   ├── FileText.png
│   ├── FolderOpen.png
│   ├── Save.png
│   └── ...
│
├── tests/                  # Автотесты
│   ├── tests_main.py
│   ├── tests_runner_app.py
│   └── tests.spec
│
├── build/                  # Временные файлы сборки (генерируется)
├── dist/                   # Собранный exe (генерируется)
└── test_reports/           # Отчеты тестирования
```

### Описание основных модулей

#### `main.py`
Точка входа приложения. Инициализирует IDEF0App и запускает главный цикл.

#### `app.py`
Содержит основной класс `IDEF0App`, который управляет:
- Созданием и отображением GUI
- Обработкой событий мыши и клавиатуры
- Управлением холстом и отрисовкой элементов
- Файловыми операциями (сохранение/загрузка)
- Undo/Redo функциональностью

#### `models.py`
Определяет модели данных:
- `Block` - Модель функционального блока
- `Arrow` - Модель стрелки с маршрутизацией
- `LayerManager` - Менеджер иерархических уровней

#### `properties.py`
Реализует панель свойств (`PropertiesPanel`):
- Отображение и редактирование свойств блоков
- Отображение и редактирование свойств стрелок
- Управление цветами, размерами, текстом

#### `styles.py`
Содержит настройки стилей:
- `Colors` - Цветовые палитры (светлая/темная тема)
- `Dimensions` - Размеры элементов интерфейса
- `Fonts` - Шрифты приложения

## 🎮 Использование

### Создание новой диаграммы

1. Запустите приложение
2. Нажмите кнопку "Новый" в панели инструментов или используйте `Ctrl+N`

### Добавление блоков

1. Нажмите на иконку "Square" (квадрат) в боковой панели
2. Кликните на холсте в месте, где хотите разместить блок
3. Или перетащите блок из панели инструментов на холст

### Редактирование блоков

1. Выберите блок кликом мыши
2. В правой панели свойств отредактируйте:
   - Название
   - Код (например, A0, A1, A2)
   - Тип элемента
   - Описание
   - Позицию и размер
   - Цвет заливки
   - Толщину границы

### Создание стрелок

1. Выберите инструмент "Стрелка" или нажмите соответствующую кнопку
2. Кликните на начальном блоке (или свободной точке)
3. Перетащите до конечного блока (или свободной точки)
4. Стрелка автоматически проложит маршрут с обходом блоков

### Редактирование стрелок

1. Выберите стрелку кликом
2. В панели свойств можно изменить:
   - Цвет стрелки
   - Толщину линии
   - Стиль линии (сплошная, пунктирная, точечная)
   - Текст на стрелке

### Работа с иерархией

1. Двойной клик по блоку открывает его детализацию (следующий уровень)
2. Кнопка "Выход" возвращает на уровень выше
3. Панель слоев показывает текущий путь в иерархии

### Сохранение и загрузка

- **Сохранение:** `Ctrl+S` или кнопка "Сохранить"
- **Сохранить как:** `Ctrl+Shift+S` или кнопка "Сохранить как"
- **Открытие:** `Ctrl+O` или кнопка "Открыть"

Файлы сохраняются в формате JSON.

## ⌨️ Горячие клавиши

| Действие | Горячая клавиша |
|----------|----------------|
| Новый проект | `Ctrl+N` |
| Открыть | `Ctrl+O` |
| Сохранить | `Ctrl+S` |
| Сохранить как | `Ctrl+Shift+S` |
| Отменить | `Ctrl+Z` |
| Повторить | `Ctrl+Y` |
| Копировать | `Ctrl+C` |
| Вставить | `Ctrl+V` |
| Удалить | `Delete` |
| Увеличить масштаб | `Ctrl+Plus` или `Ctrl+Wheel Up` |
| Уменьшить масштаб | `Ctrl+Minus` или `Ctrl+Wheel Down` |
| Сброс масштаба | `Ctrl+0` |

## 💾 Формат файлов

Проекты сохраняются в формате JSON со следующей структурой:

```json
{
  "version": "1.0",
  "blocks": [
    {
      "id": 1,
      "name": "Название блока",
      "code": "A0",
      "element_type": "Процесс",
      "description": "Описание",
      "x": 150,
      "y": 150,
      "width": 150,
      "height": 50,
      "color": "#E3F2FD",
      "border_width": 2,
      "parent_id": null
    }
  ],
  "arrows": [
    {
      "id": 1,
      "from_block_id": 1,
      "to_block_id": 2,
      "from_side": "right",
      "to_side": "left",
      "color": "#000000",
      "width": 2,
      "style": "solid",
      "text": ""
    }
  ]
}
```

## 🔧 Разработка

### Добавление новых функций

1. **Новые типы элементов:** Расширьте список в `properties.py` в методе `create_select_field`
2. **Новые стили стрелок:** Добавьте в `properties.py` в список стилей
3. **Новые темы:** Расширьте класс `Colors` в `styles.py`

### Архитектура приложения

- **MVC-подобная архитектура:**
  - **Models** (`models.py`) - Модели данных
  - **View** (`app.py`, `properties.py`) - Представление (GUI)
  - **Controller** (`app.py`) - Контроллер событий

### Расширение функциональности

Пример добавления нового инструмента:

```python
# В app.py, метод setup_sidebar
new_tool_btn = self.create_toolbar_button(sidebar, "Новый инструмент")
new_tool_btn.configure(command=self.on_new_tool_clicked)

# Добавить обработчик
def on_new_tool_clicked(self):
    self.current_mode = "new_tool"
    # Ваша логика
```

## 🧪 Тестирование

### Запуск тестов

```bash
cd tests
python tests_main.py
```

Или через собранный exe тестов:
```bash
dist\tests.exe
```

### Структура тестов

Тесты находятся в папке `tests/`:
- `tests_main.py` - Основной файл с тестами
- `tests_runner_app.py` - GUI для запуска тестов

## ❗ Решение проблем

### Ошибка "No module named 'app'"

**Решение:**
1. Убедитесь, что вы находитесь в директории `IDEF0_editor`
2. Запускайте через `python main.py`, а не через `python -m main`

### Ошибка импорта PIL/Pillow

**Решение:**
```bash
pip install Pillow
```

### Приложение не запускается (exe)

**Решение:**
1. Пересоберите exe: `python rebuild.py`
2. Убедитесь, что все зависимости установлены перед сборкой
3. Проверьте, что `img/` папка присутствует

### Ошибки при сборке exe

**Решение:**
1. Убедитесь, что PyInstaller установлен: `pip install pyinstaller`
2. Проверьте файл `main.spec` на корректность
3. Запустите сборку с подробным выводом: `pyinstaller --log-level=DEBUG main.spec`

### Стрелки не прокладываются корректно

**Решение:**
- Убедитесь, что блоки не перекрываются слишком сильно
- Попробуйте увеличить отступы между блоками
- Используйте ручную настройку точек прикрепления

### Проблемы с сохранением/загрузкой

**Решение:**
- Проверьте права доступа к папке
- Убедитесь, что формат файла корректный (JSON)
- Проверьте консоль на наличие ошибок

## 📚 Техническая документация

Подробная техническая документация находится в файле [`DOCUMENTATION.md`](IDEF0_editor/DOCUMENTATION.md).

Включает:
- Архитектуру приложения
- Описание API классов
- Алгоритмы (маршрутизация, прикрепление и т.д.)
- Формат данных
- Руководство по расширению функциональности

## 📝 Лицензия

[Укажите вашу лицензию здесь]

## 👥 Авторы

[Укажите авторов проекта]

## 📞 Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем"
2. Проверьте логи в консоли
3. Создайте issue с описанием проблемы и версией Python/ОС

---

**Версия:** 3.6  
**Последнее обновление:** 16.12.25
